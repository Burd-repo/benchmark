ARG CUDA_BASE_IMAGE
FROM ${CUDA_BASE_IMAGE}

ARG CUDA_BASE_IMAGE
ARG BURD_GATE_MODE
RUN case "$CUDA_BASE_IMAGE" in *@sha256:????????????????????????????????????????????????????????????????) ;; *) exit 64 ;; esac \
    && case "$BURD_GATE_MODE" in report|stubborn) ;; *) exit 64 ;; esac

ENV BURD_GATE_MODE=${BURD_GATE_MODE}

COPY --chmod=0555 gate-entrypoint.sh /usr/local/bin/burd-physical-nvidia-gate

ENTRYPOINT ["/usr/local/bin/burd-physical-nvidia-gate"]
